fastapi==0.136.1        # Pulls starlette ≥0.49 transitively (CVE-2025-54121, CVE-2025-62727).
uvicorn[standard]==0.32.1
SQLAlchemy==2.0.36
greenlet==3.2.4         # required by SQLAlchemy async extension
pydantic==2.10.3
python-multipart==0.0.31 # CVE-2026-{53538,53539,53540} + prior 0.0.27 advisories
email-validator==2.2.0
PyJWT==2.13.0           # CVE-2026-32597; PYSEC-2026-{175,177,178,179}
passlib[bcrypt]==1.7.4
bcrypt==4.3.0           # Pinned: passlib 1.7.4 is incompatible with bcrypt 5.x.
                        # Still needed to verify legacy bcrypt hashes; new hashes use argon2.
argon2-cffi==23.1.0     # Primary password hash. OWASP-recommended over bcrypt.

# Database
asyncpg==0.31.0           # async driver used by the FastAPI app
psycopg2-binary==2.9.12   # sync driver used by Alembic migrations
alembic==1.18.4

# Config
python-dotenv==1.2.2    # CVE-2026-28684

# Async SMTP
aiosmtplib==5.1.0

# Rate limiting
slowapi==0.1.9

# Image validation / sanitisation for uploads
Pillow==12.2.0          # CVE-2026-{25990,40192,42308,42310,42311}

# Async file I/O (uploads write through aiofiles so the event loop
# isn't blocked on disk while a multi-MB image lands).
aiofiles==25.1.0
