[pytest]
# Add project root to sys.path so `from app import app` resolves
# regardless of how pytest is invoked (`pytest`, `python -m pytest`,
# or via CI runners that don't pass through `python -m`).
pythonpath = .
testpaths = tests
